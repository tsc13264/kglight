includes:
  - configs/tsc/base.yml

model:
  name: dqn
  # train_model: True
  train_model: False
  epsilon: 0.1
  epsilon_decay: 0.995
  epsilon_min: 0.01
  one_hot: True
  phase: True
  # batch_size: 2
  
trainer:
  learning_start: 1000
  # learning_start: 2
  buffer_size: 5000
  # episodes: 1
  # load_model: False
  load_model: True
  # test_steps: 20000
